In the camera settings, set the following
Opaque Texture - ON
Depth Texture - ON
Culling Mask - Everything

For the Water Bounding Box please use a box collider and set Is Trigger to true
For the Water Bounding Box set the mesh renderer materials to 
In the Post Processing Volume set the parameters as I have set them
In this example I set Player to the camera but it can be set to a Player that is controlled by a controller.